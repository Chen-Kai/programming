#include <stdio.h>
#include <stdlib.h>
#include "a.h"

int main(int argc, char **argv) {
	// displayTypes();
	// testEnum();
	// testIO();
	// print_args(argc, argv);
	// printf("sum_of_args(3, 1, 2, 3) == %d\n", sum_of_args(3, 1, 2, 3));
	testArrQueue();
	return EXIT_SUCCESS;
}
